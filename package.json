{
  "name": "ai-sdk-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --recursive run build",
    "lint": "pnpm --recursive run lint"
  },
  "dependencies": {
    "wink-bm25-text-search": "^1.0.0",
    "ai": "^6.0.42",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "typescript": "^5.5.4"
  }
}